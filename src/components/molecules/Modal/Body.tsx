import styled from 'styled-components'

const Body = styled('div')`
  margin: auto;
  height: calc(100% - 64px);
  width: 100%;
  padding: 24px;
  position: relative;
  overflow-y: auto;
`

export default Body
