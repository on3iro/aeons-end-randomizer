import styled from 'styled-components/macro'

import List from 'components/atoms/_styled_/List'

const TileBodyWrapper = styled('div')`
  ${List} {
    padding-top: 8px;
  }
`

export default TileBodyWrapper
