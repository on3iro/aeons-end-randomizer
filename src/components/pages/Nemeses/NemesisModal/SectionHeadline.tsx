import styled from 'styled-components'
import H2 from '../../../H2'

const SectionHeadline = styled(H2)`
  color: ${props => props.theme.colors.turnOrderCards['nemesis']['normal']};
  font-weight: 300;
  margin: 16px 0;
`

export default SectionHeadline
