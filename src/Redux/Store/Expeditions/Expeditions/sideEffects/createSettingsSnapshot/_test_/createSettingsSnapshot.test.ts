import { createSettingsSnapshot } from '../index'

describe('createSettingsSnapshot()', () => {
  it.todo('should call handleExistingSettingsSnapshot()')

  it.todo('should call handleNewSettingsSnapshot()')
})
